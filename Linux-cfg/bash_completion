
complete -F _download -A file download

_download()
{
	local cur prev opts
	COMPREPLY=()
	cur="${COMP_WORDS[COMP_CWORD]}"
	prev="${COMP_WORDS[COMP_CWORD-1]}"
	opts="uboot scpu nfs all stage1 stage2 -a -b -d -f -r"

	case ${cur} in
	*)  COMPREPLY=( $(compgen -W "${opts}" -- ${cur}) ) ;;
	esac
}
